# Copy this file to .env and set your NewsAPI key
NEWS_API_KEY=your_newsapi_key_here
# In production, set your frontend origin to restrict CORS, e.g. https://yourdomain.com
ALLOWED_ORIGIN=https://your-frontend.example
# NODE_ENV defaults to 'development' locally; set to 'production' in production
NODE_ENV=development

# If your frontend is built to use a specific API base URL, set REACT_APP_API_URL in your frontend .env
# Example in frontend .env: REACT_APP_API_URL=https://api.yourdomain.com
