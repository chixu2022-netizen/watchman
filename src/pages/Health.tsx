import React from 'react';
import CategoryPageTemplate from './CategoryPageTemplate';

const Health: React.FC = () => {
  return <CategoryPageTemplate category="health" title="Health" />;
};

export default Health;
